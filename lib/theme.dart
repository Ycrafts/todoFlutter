import 'package:flutter/material.dart';

class ColorPalette {
  // Light Theme (Keeping these as is)
  static const Color primaryColor = Colors.blue;
  static const Color textColorPrimary = Colors.black87;
  static const Color textColorSecondary = Colors.grey;
  static const Color errorColor = Colors.red;
  static const Color cardBackgroundColor = Colors.white;
  static const Color iconColor = Colors.black54;

  // Dark Theme - Updated for a darker approach
  static const Color backgroundColorDark = Color(0xFF1E273B); // Dark blue-grey
  static const Color primaryColorDark = Color(0xFF384766); // Slightly muted dark blue
  static const Color appBarColorDark = Color(0xFF1E273B); // Same as background for a seamless app bar
  static const Color textColorPrimaryDark = Color(0xFFE0E6ED); // Light grey for better contrast
  static const Color textColorSecondaryDark = Color(0xFF9CA3AF); // Medium grey-blue for less important text
  static const Color textColorHintDark = Color(0xFF6B7280); // Darker grey-blue for hints
  static const Color textColorLinkDark = Color(0xFF64B5F6); // Keep a brighter blue for links/actions
  static const Color errorColorDark = Color(0xFFE57373); // A slightly different shade of red for errors
  static const Color cardBackgroundColorDark = Color(0xFF28374A); // Slightly lighter than background for card separation
  static const Color iconColorDark = Color(0xFF9CA3AF); // Medium grey-blue for icons
  static const Color borderColorDark = Color(0xFF4A5568); // Dark grey-blue for borders
  static const Color buttonColorDark = Color(0xFF384766); // Same as primary for button background
  static const Color buttonTextColorDark = Color(0xFFE0E6ED); // Light grey for button text
}